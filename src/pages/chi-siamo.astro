---
import BaseLayout from '../components/layout/BaseLayout.astro';
---

<BaseLayout
  title="Chi Siamo - La Festa delle Medie"
  description="Scopri chi siamo e cosa facciamo"
>
  <div class="page-content">
    <div class="container">
      <!-- Hero -->
      <header class="page-header">
        <h1 class="page-title">Chi Siamo</h1>
        <p class="page-subtitle">
          La Festa delle Medie e un organismo culturale pubblico e visionario,
          che da oltre cinquant'anni fa della ricerca artistica uno spazio di relazione,
          programmando attivita locali e internazionali.
        </p>
      </header>

      <!-- Immagine principale -->
      <div class="page-image">
        <img src="/images/event1.jpg" alt="La Festa delle Medie" />
      </div>

      <!-- Contenuto -->
      <div class="content-grid">
        <div class="content-block">
          <h2 class="content-title">Eventi</h2>
          <p class="content-text">
            Programmiamo eventi che spaziano dalla musica dal vivo
            alle installazioni artistiche, dal teatro sperimentale alle
            proiezioni cinematografiche.
          </p>
        </div>

        <div class="content-block">
          <h2 class="content-title">Artisti</h2>
          <p class="content-text">
            Sosteniamo artisti emergenti offrendo spazi e
            opportunita per esprimere la loro creativita e sperimentare
            nuovi linguaggi espressivi.
          </p>
        </div>

        <div class="content-block">
          <h2 class="content-title">Comunita</h2>
          <p class="content-text">
            Creiamo comunita attraverso workshop, laboratori
            e momenti di incontro aperti a tutti, dove l'arte diventa
            strumento di connessione sociale.
          </p>
        </div>
      </div>

      <!-- CTA -->
      <div class="page-cta">
        <a href="/eventi" class="btn-outline">Scopri gli Eventi</a>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .page-content {
    padding-top: calc(72px + var(--space-3xl));
    padding-bottom: var(--space-3xl);
  }

  .page-header {
    margin-bottom: var(--space-2xl);
    max-width: 800px;
  }

  .page-title {
    font-size: var(--text-5xl);
    font-weight: 700;
    margin-bottom: var(--space-lg);
  }

  .page-subtitle {
    font-size: var(--text-xl);
    line-height: var(--leading-relaxed);
    color: var(--color-gray-dark);
    margin: 0;
  }

  .page-image {
    margin-bottom: var(--space-3xl);
  }

  .page-image img {
    width: 100%;
    aspect-ratio: 21/9;
    object-fit: cover;
  }

  .content-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-2xl);
    margin-bottom: var(--space-3xl);
  }

  .content-block {
    padding: 0;
  }

  .content-title {
    font-size: var(--text-2xl);
    font-weight: 600;
    margin-bottom: var(--space-md);
  }

  .content-text {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--color-gray-dark);
    margin: 0;
  }

  .page-cta {
    text-align: center;
  }

  @media (max-width: 1024px) {
    .content-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .page-content {
      padding-top: calc(65px + var(--space-2xl));
    }

    .page-title {
      font-size: var(--text-3xl);
    }

    .page-subtitle {
      font-size: var(--text-lg);
    }

    .page-image img {
      aspect-ratio: 16/9;
    }

    .content-grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .content-title {
      font-size: var(--text-xl);
    }
  }
</style>
